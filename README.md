# 🩺 DFU Detector Web App

A Flask-based deep learning app that detects Diabetic Foot Ulcers using a TensorFlow EfficientNet model.

## ⚙️ Tech Stack
- Flask (Backend)
- TensorFlow (Model)
- HTML + CSS (Frontend)

## 🧠 Run Locally
```bash
git clone https://github.com/MAllIkArJUNfit/DFU-Detector-WebApp.git
cd DFU-Detector-WebApp
pip install -r requirements.txt
python app.py
